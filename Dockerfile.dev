# 개발용 Dockerfile
FROM node:18-alpine

# 작업 디렉토리 설정
WORKDIR /app

# package.json과 yarn.lock 복사
COPY package.json yarn.lock ./

# 의존성 설치 (개발 의존성 포함)
RUN yarn install --frozen-lockfile

# 소스 코드 복사
COPY . .

# 개발 서버 포트 노출
EXPOSE 8080

# 개발 서버 실행
CMD ["yarn", "serve"]
